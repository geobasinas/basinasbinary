{
   "extends": ["next/core-web-vitals", "next/typescript"],
  "overrides": [
    {
      "files": ["components/ui/**/*"],
      "rules": {
        // Disable all rules for files in the /components/ui directory
        "all": "off"
      }
    }
  ]
}